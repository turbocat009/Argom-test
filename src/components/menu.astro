<section id="contextMenu" class="none"></section>

<style>
    section {
        width: 100%;
        height: 100%;
        background-color: #9998;
        backdrop-filter: blur(50px);
        position: absolute;
        z-index: -1;
    }

    .divOpen {
        animation: backAppear 0.5s ease-in-out;
    }

    .divClose {
        animation: backDisappear 0.5s ease-in-out;
    }

    @keyframes backAppear {
        0% {
            background-color: #0000;
        }
        100% {
            background-color: #9998;
        }
    }

    @keyframes backDisappear {
        0% {
            background-color: #9998;
        }
        100% {
            background-color: #0000;
        }
    }
</style>
